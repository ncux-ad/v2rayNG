<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/empty_servers_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/padding_spacing_dp32"
    android:visibility="gone">

    <ImageView
        android:layout_width="@dimen/empty_icon_size"
        android:layout_height="@dimen/empty_icon_size"
        android:contentDescription="@null"
        android:src="@drawable/ic_add_24dp"
        app:tint="?attr/colorOnSurfaceVariant" />

    <TextView
        android:id="@+id/empty_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_spacing_dp16"
        android:text="@string/empty_servers_title"
        android:textAppearance="?attr/textAppearanceTitleMedium" />

    <TextView
        android:id="@+id/empty_hint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_spacing_dp8"
        android:gravity="center"
        android:text="@string/empty_servers_hint"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/empty_action"
        style="@style/Widget.Material3.Button.TonalButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/padding_spacing_dp24"
        android:minHeight="@dimen/min_touch_target_dp48"
        android:text="@string/empty_servers_action"
        app:icon="@drawable/ic_add_24dp"
        app:iconGravity="textStart"
        app:iconPadding="@dimen/padding_spacing_dp8"
        app:iconTint="?attr/colorOnSecondaryContainer" />

</LinearLayout>
